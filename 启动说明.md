# 无碍智行 v1.0 本地启动说明

> 更新时间：2026-08-22
> 适用环境：Windows + Docker Desktop + WSL2

## 1. 环境要求（已完成）

- Docker Desktop 4.87（已安装，已配置国内镜像加速）
- WSL2 + Ubuntu（已安装）
- 项目根目录 `.env` 已配置（高德 Key、DeepSeek Key、数据库密码、JWT_SECRET）
- 前端本地开发建议使用 Node 22（Docker 构建固定 Node 22；便携版在 `D:\node22\node-v22.23.2-win-x64`，`frontend/.nvmrc` 已声明 22）

## 2. 启动步骤

1. 打开 **Docker Desktop**，等左下角变成绿色 Running。
2. 在项目根目录打开 PowerShell：

   ```powershell
   cd C:\Users\cho\Desktop\导航\BarrierFreeCompus
   ```

3. 启动容器：

   - **第一次启动 / 改过代码 / 更新过 .env**：

     ```powershell
     docker compose up -d --build
     ```

   - **日常再次启动**（镜像已存在，无需重新构建）：

     ```powershell
     docker compose up -d
     ```

4. 等待 1~2 分钟，确认状态：

   ```powershell
   docker compose ps
   ```

   期望看到 `db`、`backend` 为 healthy，`frontend` 为 running。

5. 浏览器打开 **http://localhost:8080** 即可使用。

## 3. 登录账号

| 角色 | 账号 | 密码 |
|---|---|---|
| 校园用户 | `demo_user` | `Demo@12345` |
| 管理员 | `demo_admin` | `Admin@12345` |

> 仅限本地演示，正式部署前必须更换或停用。

## 4. 常用命令

| 操作 | 命令 |
|---|---|
| 查看容器状态 | `docker compose ps` |
| 查看后端日志 | `docker compose logs -f backend` |
| 查看数据库日志 | `docker compose logs --tail 100 db` |
| 停止容器（保留数据） | `docker compose down` |
| 重启 | `docker compose up -d` |

⚠️ **不要使用 `docker compose down -v`**，会删除 `postgres-data` 数据卷，数据库数据不可恢复。

## 5. 访问地址

| 服务 | 地址 |
|---|---|
| 前端页面 | http://localhost:8080 |
| 后端直连 | http://localhost:8081 |
| 健康检查 | http://localhost:8080/actuator/health |
| Swagger 文档 | http://localhost:8080/swagger-ui.html |

## 6. 需要的外部 API（均已配置到 .env）

| 配置项 | 来源 | 说明 |
|---|---|---|
| `AMAP_JS_KEY` / `AMAP_SECURITY_JS_CODE` | 高德开放平台（当前为共享 Key） | 地图底图；若地图空白，需自己申请 Key 并把 `localhost` 加入域名白名单 |
| `AI_BASE_URL` / `AI_API_KEY` / `AI_MODEL_NAME` | DeepSeek（当前为你自己的 Key） | 智能助手的真实模型调用；Key 失效时 AI 自动降级，不影响手工路线 |
| `DB_PASSWORD` / `JWT_SECRET` | 本地生成 | 数据库密码与 JWT 签名密钥，无需申请 |

> 密钥内容只存在于根目录 `.env`（已被 .gitignore 排除），请勿提交或发到聊天中。

## 7. 常见问题

### 地图空白

- 检查 `.env` 中两个高德变量是否填写。
- 检查该 Key 的域名白名单是否包含 `localhost`。
- 检查浏览器控制台 `/_AMapService` 请求是否带上了 `jscode`。

### AI 助手不可用 / 提示降级

- 检查 `.env` 中 `AI_ENABLED=true` 且三个 AI 变量齐全（缺任一后端会拒绝启动）。
- Key 失效或限流时，手工路线、统计、管理功能不受影响。
- 想先关掉 AI，把 `AI_ENABLED=false` 后重新 `docker compose up -d --build`。

### 拉取镜像慢 / 失败

- 已在 Docker 配置中加入国内加速源（`docker.m.daocloud.io` 等），原配置备份在 `C:\Users\cho\.docker\daemon.json.bak`。
- 若仍失败，可换用其他可用加速源后重启 Docker Desktop。

### 提示 `no configuration file provided: not found`

- 说明当前终端不在项目根目录，Docker Compose 找不到 `docker-compose.yml`。
- 先执行 `cd C:\Users\cho\Desktop\导航\BarrierFreeCompus` 再运行 `docker compose up -d`。

### 提示 `failed to connect to the docker API` / `dockerDesktopLinuxEngine`

- 说明 Docker Desktop 引擎没在运行（可能被关闭或开机后还没启动）。
- 打开 Docker Desktop，等左下角变绿后再执行 `docker compose up -d`。

## 8. 停止项目

```powershell
docker compose down
```

再次使用按第 2 节步骤启动即可，数据不会丢失。

## 9. 数据库备份与恢复

### 备份（pg_dump）

```powershell
powershell -ExecutionPolicy Bypass -File scripts\backup-db.ps1
```

- 备份文件保存到项目根目录 `backups/`（已加入 .gitignore，不会提交）
- 文件名带时间戳：`barrierfreecampus-YYYYMMDD-HHMMSS.dump`
- 默认保留最近 14 份，可用 `-Keep 30` 自定义

### 恢复（谨慎，会覆盖当前数据库）

```powershell
cmd /c "docker compose exec -T -e PGPASSWORD db pg_restore -U barrierfree -d barrierfreecampus --clean --if-exists < backups\文件名.dump"
```

> 恢复会先清空再写入，操作前请确认，并建议先做一次新备份。

### 定时备份

已配置 Windows 计划任务 `BarrierFreeCampus-DB-Backup`：每天 03:00 自动执行备份脚本。

- 查看：`schtasks /query /tn "BarrierFreeCampus-DB-Backup"`
- 手动触发：`schtasks /run /tn "BarrierFreeCampus-DB-Backup"`
- 删除：`schtasks /delete /tn "BarrierFreeCampus-DB-Backup" /f`
