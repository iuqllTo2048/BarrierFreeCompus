# 常用修复 / 变更 Prompt

## Bug 先分析不修改
```text
当前出现错误。先不要修改任何代码。
请读取错误日志和相关文件，给出：
1. 最可能根因；
2. 证据；
3. 受影响文件；
4. 最小修复方案；
5. 是否涉及数据库/依赖/配置；
6. 修复风险。
等待我确认后再改。
```

## 只修当前 Bug
```text
按刚才确认的最小方案修复。
禁止借机重构无关模块，禁止升级无关依赖，禁止进入下一 Stage。
修复后执行最小必要测试并报告。
```

## 需求变更
```text
这是用户主动提出的需求变更，不是让你直接编码。
先做影响分析：数据库、后端 API、前端、A*、AI Tool、测试、文档、当前 Stage。
给出最小变更方案，等待确认。
```

## Codex 越界
```text
停止。检查 AGENTS.md 和当前 Stage Prompt。
列出你刚才做了哪些超出当前 Stage 的改动。
不要删除或回滚，先等待我决定如何处理。
```

## 项目复盘
```text
使用 $barrier-free-project-recap。
基于当前真实代码做完整复盘。
我主要是 Vibe Coding，请从小白视角讲清楚，但必须指出真实文件、类、方法和数据流。
```

## 外部配置核对
```text
不要修改代码。
扫描 .env.example、application 配置和前端 env。
列出当前所有外部配置：是否必填、从哪里申请、应填在哪里、当前是否缺失、缺失影响、是否可能泄密。
不要输出真实密钥值。
```
